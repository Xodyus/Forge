"""Forge — a deterministic, local-first event-replay and compute engine.

This top-level package only re-exports nothing yet; it is a namespace anchor for the
subpackages listed in the component inventory (docs/spec/part-03-architecture.md §38):
api, cli, domain, planner, coordinator, metadata, artifacts, protocol, transport,
worker, kernels, datasets, observability, bench, testing.

Read docs/spec/INDEX.md before implementing any component.
"""
